"""
A series of packages/Django apps that load Philadelphia-specific data.

These packages, as applicable, have the following modules:
    * .api: communicates directly with the external data source
    * .adapter: adapts data from the external data source's format into a model
    * .models: local representations of the external data

"""
